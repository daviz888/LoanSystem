@model IEnumerable<LoanSystem.Domain.Entities.Interest>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.InterestRate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InterestYear)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.InterestRate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.InterestYear)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InterestID }) |
            @Html.ActionLink("Details", "Details", new { id=item.InterestID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.InterestID })
        </td>
    </tr>
}

</table>
